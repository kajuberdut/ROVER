[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rover-cli"
version = "0.1.0"
description = "Command line interface for ROVER"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
rover-cli = "rover_cli.main:main"
